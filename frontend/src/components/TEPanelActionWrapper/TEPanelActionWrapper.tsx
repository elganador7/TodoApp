import styled from 'styled-components'

const TEPanelActionWrapper = styled.div`
	display: flex;
	flex-wrap: wrap;
	justify-content: flex-end;
	align-items: center;
`

export default TEPanelActionWrapper
