import styled from 'styled-components'

const TEPopupTitle = styled.h1`
	text-align: center;
	font-size: 26px;
	margin-top: 0px;
	margin-bottom: 15px;
	color: ${(props) => props.theme.primary};
`

export default TEPopupTitle
